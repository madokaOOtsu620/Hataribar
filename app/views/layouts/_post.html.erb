<div>
  <strong>投稿日時：</strong>
  <span><%= post.created_at.to_s(:datetime_jp) %></span><br>
  <strong>更新日時：</strong>
  <span><%= post.updated_at.to_s(:datetime_jp) %></span>
  <p></p>
</div>

<div class="field">
  <strong>回答者：</strong>
  <span>
    <% if @post.user.is_deleted == false %>
      <%= @post.user.user_name %>
    <% else %>
      退会済みユーザー
    <% end %>
  </span>
</div>

<div class="field">
  <strong>回答者の年齢：</strong>
  <span><%= post.user.age %>代</span>
  <p></p>
</div>

<div class="field">
  <h4><業種></h4>
  <p><%= post.industry.industry %></p>
</div>
<div class="field">
  <h4><仕事内容></h4>
  <p><%= post.answer_what %></p>
</div>
<div class="field">
  <h4><雇用形態></h4>
  <p><%= post.answer_employment_status %></p>
</div>
<div class="field">
  <h4><勤務形態></h4>
  <p><%= post.answer_working_style %></p>
</div>
<div class="field">
  <h4><収入面></h4>
  <p><%= post.answer_income %></p>
</div>
<div class="field">
  <h4><この仕事に就くルート></h4>
  <p><%= post.answer_how %></p>
</div>
<div class="field">
  <h4><必要な資格やスキル></h4>
  <p><%= post.answer_skill %></p>
</div>

<% if post.answer_why == "" %>
<% else %>
<div class="field">
  <h4><なぜこの仕事をやろうと思ったか？></h4>
  <p><%= post.answer_why %></p>
</div>
<% end %>

<% if post.answer_aptitude == "" %>
<% else %>
<div class="field">
  <h4><適性、性格による向き不向き></h4>
  <p><%= post.answer_aptitude %></p>
</div>
<% end %>

<% if post.answer_future == "" %>
<% else %>
<div class="field">
  <h4><この仕事の将来性は？></h4>
  <p><%= post.answer_future %></p>
</div>
<% end %>

<% if post.answer_advantage == "" %>
<% else %>
<div class="field">
  <h4><メリット、やりがい></h4>
  <p><%= post.answer_advantage %></p>
</div>
<% end %>

<% if post.answer_free == "" %>
<% else %>
<div class="field">
  <h4><最後に></h4>
  <p><%= post.answer_free %></p>
</div>
<% end %>